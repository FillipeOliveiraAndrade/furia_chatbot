services:
  - type: web
    name: furia-chatbot-api
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn app.services.main:app --host 0.0.0.0 --port 8000
    rootDir: backend
